# Decisions

- **2026-04-06** — `--ngo-accent-h`: defined as `var(--ngo-accent-text)` so profit label and manifesto expand toggle gradients use the same text-safe accent on light contexts; avoids an extra hue to maintain.
